spring:
  application:
    name: isep-config-server
  cloud:
    config:
      server:
        git:
          uri: ${DBE_REPOS:https://github.com/ssidibe-ept/dbe-p6-repos.git}
          default-label: main
  profiles:
    active: ${SERVEUR_PROFILE:dev}
server:
  port: ${CFG_SERVER_PORT:8888}